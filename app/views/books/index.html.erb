<% @books.each do |book| %>
<div class = "contain">
<div align ="center" class="category">
<p><%= book.title %></p>
<%= link_to image_tag(book.image_url), user_book_path(book.user,book) %></div>
<% end %>
</div>
